# VN TTS Studio - Requirements
# ============================
# 
# These are the core dependencies for the application.
# Heavy ML libraries (torch, librosa, etc.) should be installed separately
# or copied to the _libs folder for PyInstaller builds.

# UI Framework
customtkinter>=5.2.0
pillow>=10.0.0

# Network & HTTP
requests>=2.31.0
urllib3>=2.0.0
aiohttp>=3.8.0
websockets>=12.0

# Document Processing
python-docx>=0.8.11

# Google Gemini (optional - for Gemini TTS features)
google-genai>=0.3.0

# Audio (optional - for audio playback)
pyaudio>=0.2.13

# YAML Configuration
pyyaml>=6.0.0

# ==================================
# HEAVY LIBRARIES (Install separately)
# ==================================
# Uncomment these if you want to install them via pip
# They are not included in PyInstaller builds by default

# PyTorch (CPU or CUDA version)
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

# Audio Processing
# librosa>=0.10.0
# soundfile>=0.12.0

# VN TTS Engine
# neucodec>=0.0.4
# phonemizer>=3.3.0  # Also requires eSpeak NG

# ONNX Runtime (for ONNX codec)
# onnxruntime>=1.16.0

# GGUF Model Support (optional)
# llama-cpp-python>=0.2.0

